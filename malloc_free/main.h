#ifndef MAIN_H
#define MAIN_H

/*
 * File: main.h
 * Desc: Header file containing declarations for the  function
 * used in the malloc_free  directory.
 */

int _putchar(char c);
char *create_array(unsigned int size, char c);
char *_strdup(char *str);

#endif
